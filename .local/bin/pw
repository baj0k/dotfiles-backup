#!/bin/sh

password=$(find $PASSWORD_STORE_DIR -type f -name '*.gpg' -not -path '*/otp*' | sed 's/.*\/password-store\///' |  sed 's/.gpg$//' | dmenu -i -p "Pass:")
[ -n "$password" ] && pass show --clip "$password"
