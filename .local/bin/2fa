#!/bin/sh

password=$(find $PASSWORD_STORE_DIR/otp/ -type f -name '*.gpg' | sed 's/.*\/otp\///' |  sed 's/.gpg$//' | dmenu -i -p "Select OTP:")
[ -n "$password" ] && pass otp --clip otp/$password && otp=$(xclip -o -selection c) && notify-send -t 10000 $otp
